name: Build & Deploy Chihiro Web

on:
  push:
    branches: [main]
    paths:
      - 'src/**'
      - 'public/**'
      - 'package*.json'
      - 'Dockerfile'
      - 'astro.config.mjs'
  workflow_dispatch:

env:
  IMAGE: ghcr.io/john6810/chihiro-web

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Login ghcr.io
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Get short SHA
        id: sha
        run: echo "short=$(git rev-parse --short HEAD)" >> $GITHUB_OUTPUT

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build & Push
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          tags: |
            ${{ env.IMAGE }}:${{ steps.sha.outputs.short }}
            ${{ env.IMAGE }}:latest
          build-args: |
            SUPABASE_URL=${{ secrets.SUPABASE_URL }}
            SUPABASE_KEY=${{ secrets.SUPABASE_KEY }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Checkout argocd-apps
        uses: actions/checkout@v4
        with:
          repository: john6810/argocd-apps
          token: ${{ secrets.GITOPS_PAT }}
          path: argocd-apps

      - name: Update image tag
        run: |
          SHA="${{ steps.sha.outputs.short }}"
          FILE="argocd-apps/apps/chihiro-web/deployment.yaml"
          sed -i "s|image: ghcr.io/john6810/chihiro-web:.*|image: ghcr.io/john6810/chihiro-web:${SHA}|" "$FILE"

      - name: Commit & Push to argocd-apps
        run: |
          cd argocd-apps
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add apps/chihiro-web/deployment.yaml
          git commit -m "chore(chihiro-web): update image to ${{ steps.sha.outputs.short }}" || echo "nothing to commit"
          git push
